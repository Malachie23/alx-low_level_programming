#include "main.h"
#include <stdio.h>

/**
 * mul - checks multiplication of two numbers
 * @a: value one
 * @b: value two
 * Return: always 0.
 */
int mul(int a, int b)
{
	int r = a * b;

	return (r);
}
