#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen - fucntion that count length
 * @s: Input
 * Return: Always length number..
 */

int _strlen(char *s)
{
	int n;

	n = strlen(s);
	return (n);
}
